#ifndef XML_H_DEFINED
#define XML_H_DEFINED


#include <sstream>
#include <string>
#include <vector>

std::string getContentFromTag(std::string str, std::string tag)
{
  auto pos = str.find(tag);
  if(pos!=std::string::npos)
  { 
      

  }
  return std::string();
}



#endif